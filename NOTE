List of descriptions for each file:

  - extract_rsl_X.py
        takes Konstantin's column output (of 5 fields)
        and extracts the last column (SL) and turns it into
        RSL and reshapes to get maps.

        X = regional (Antarctica) or global

        to do them all run:
        run_all_regional.cli or run_all_global.cli
